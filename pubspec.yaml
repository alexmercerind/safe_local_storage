name: safe_session_storage
version: 0.0.1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  path: ^1.8.2
  synchronized: ^3.0.0+2

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
  collection: ^1.16.0
